<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.sta.dao.UserMapper">


    <resultMap id="BaseResultMap" type="me.sta.entity.User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_name,password
    </sql>

	<select id="findByUsername" resultMap="BaseResultMap" parameterType="me.sta.entity.User">
		select id, user_name,password from user where user_name=#{userName,jdbcType=VARCHAR}
	</select>

    <insert id="insert" parameterType="me.sta.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        insert into user (id, user_name,password)
        values (#{id,jdbcType=INTEGER}, #{userName,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR})
    </insert>
   

</mapper>
